# Student Board API - Backend Documentation

This is the backend server for the Student Board application. It uses **Node.js**, **Express**, and **SQLite** to manage users, schedules, and announcements.

## 🚀 Getting Started

### 1. Prerequisites

Make sure you have [Node.js](https://nodejs.org/) installed.

### 2. Installation

Navigate to the server folder and install dependencies:

```bash
cd server
npm install
```
